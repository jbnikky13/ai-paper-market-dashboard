# Investo is optional: the bot must never require it for NGX updates.
# Existing implementation already uses Yahoo Finance as the final fallback.
# This marker documents the contract for the bot and keeps deployment independent of Investo.
